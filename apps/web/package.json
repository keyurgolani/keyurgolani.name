{
  "name": "@portfolio/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:registry": "node scripts/build-registry.mjs",
    "predev": "pnpm build:registry",
    "dev": "next dev",
    "prebuild": "pnpm build:registry",
    "build": "next build",
    "start": "next start",
    "typecheck": "pnpm build:registry && tsc --noEmit",
    "lint": "next lint",
    "test": "echo \"(no tests yet)\""
  },
  "dependencies": {
    "@portfolio/kit": "workspace:*",
    "@portfolio/schema": "workspace:*",
    "@portfolio/variant-editorial": "workspace:*",
    "next": "^16.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "yaml": "^2.6.1",
    "smol-toml": "^1.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.7.2"
  }
}
